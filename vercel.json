{
    "buildCommand": "pnpm install && cd packages/database && pnpm prisma generate && cd ../../apps/web && pnpm build",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "outputDirectory": "apps/web/.next"
}